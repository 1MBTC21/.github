# GitHub Repository mit SSH klonen

Mit SSH kannst du GitHub-Repositories nutzen

## Vorteil

Nach der einmaligen Einrichtung funktionieren diese Befehle in der Regel direkt:

```bash
git clone git@github.com:OWNER/REPO.git
git pull
git push
